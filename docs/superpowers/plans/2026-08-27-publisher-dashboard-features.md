# Publisher Dashboard Features Plan

Header: `superpowers:subagent-driven-development`

- [x] Add period-aware SQL aggregates and regression coverage in `apps/web/src/app/publisher/dashboard/`.
- [x] Add shareable period controls and honest period labels.
- [x] Extract the owned catalog into `apps/web/src/app/publisher/catalog-table.tsx` and reuse it in both publisher catalog views.
- [x] Add publisher-owned per-book analytics and privacy-safe retention buckets.
- [x] Add admin campaign review actions, page, and publisher notifications.
- [x] Add payout ledger read model and explicit empty settlement state.
- [x] Run web typecheck, lint, and tests; record remaining manual QA.
