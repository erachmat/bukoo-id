# SDD Ledger — Store Launch Option A

Plan: `docs/superpowers/plans/2026-08-16-store-launch-option-a.md`  
Spec: `docs/superpowers/specs/2026-08-16-store-launch-option-a-design.md`  
Started: 2026-08-16 · Mode: plan-only → GO (executing Phase 1 + 3)

## Progress

- **Task 1 (SDD docs)**: complete — spec + plan + ledger created (this file). ✅
- **Task 2 (download.epub security fix)**: complete — route now enforces auth + subscription (`apps/web/src/app/api/books/[id]/download.epub/route.ts`). ✅
- **Task 3 (shared tier helper + /me)**: complete — `apps/web/src/lib/subscription.ts` (tierFromSubscription, getUserTierFromDb, canReadBook); `apps/api/src/routes/users.ts` GET+PATCH /me now return `subscription`; `authStore.ts` UserPublicDto + subscription. ✅
- **Task 4 (seed subscription_plans + priceYearly)**: complete — `packages/db/src/seed-subscriptions.ts` (4 plans, upsert + SQL export); schema `price_yearly` added; migration `0002_wet_menace.sql` generated (single ALTER ADD COLUMN, no FTS). ✅ APPLIED TO REMOTE D1 2026-08-17 — ran `wrangler d1 execute --file=0002_wet_menace.sql`, verified `price_yearly` REAL column, executed idempotent seed upsert, verified all 4 plans (plan_BACA/PLUS/PERSONAL/FAMILY with monthly+yearly IDR).
- **Task 4b (d1_migrations tracker cleanup)**: complete 2026-08-17. ⚠️ CORRECTION to earlier claim: the tracker was NOT empty — it already contained `0000_familiar_jackpot.sql` + `0001_fts5_books.sql` (applied 2026-08-14; earlier "empty" reading was a bad query: `created_at` column doesn't exist and grep missed rows). Real gap: `0001_cultured_shiva.sql` + `0002_wet_menace.sql` were applied manually but missing from tracker → a `migrations apply` would have failed on duplicate ALTER columns. Inserted both (applied_at 2026-08-17); **verified `wrangler d1 migrations apply bukoo-db --remote` → "✅ No migrations to apply!"**. No FTS replay risk exists (0001_fts5_books already recorded).
- **Task 4c (fresh-DB FTS corrective migration 0003)**: complete 2026-08-17 — WITH an incident worth recording:
  - Created `0003_fix_fts5_triggers.sql` (drop broken `'delete'` triggers, recreate D1-safe ones) and added to drizzle journal + remote tracker.
  - ⚠️ INCIDENT: my first 0003 recreated `books_ad`/`books_au` using a plain `DELETE FROM books_fts WHERE rowid IN (...)` — this PASSED on local miniflare but **threw SQLITE_ERROR 7500 on remote D1** (D1 does not support FTS DELETE/UPDATE at all). This re-broke DELETE/UPDATE on `books` for a short window on remote. Caught via live remote write test (insert worked, delete 7500'd).
  - FIX: dropped `books_ad`/`books_au` on remote; kept only `books_ai` (insert). Rewrote `0003_fix_fts5_triggers.sql` to the **insert-only** design. Re-verified on remote: controlled insert → FTS search query returns the row; DELETE works; no 7500.
  - KEY LESSON (also added to AGENTS.md): local miniflare D1 supports FTS deletes; remote D1 does NOT. Always verify FTS trigger behavior against remote.
  - End state: `books` writes (INSERT/UPDATE/DELETE) all work on remote; FTS search works via insert-only trigger; soft-removal via `is_published=0`; hard-DELETE leaves harmless orphan FTS rows.
  - NOTE: `books` table on remote currently has 0 rows (earlier test data gone); `books_fts` has 2 orphan rows — harmless (JOIN filters them), could be cleaned by rebuilding books_fts when real books exist.
- **Task 5 (Phase 2 — Firebase App Distribution + A/B testing, code side)**: complete 2026-08-17. Installed `@react-native-firebase/app|crashlytics|remote-config` v26 (modular API — `getRemoteConfig`/`getCrashlytics`, NOT the old callable default-import); config plugins added to app.json (app/crashlytics/remote-config). New: `src/services/featureFlags.ts` (flag registry: home_layout/pricing_display/onboarding_flow + Remote Config init + zustand store), `src/hooks/useFeatureFlags.ts`, `src/services/crashReporting.ts`; wired `home_layout` (carousel vs grid) into HomeScreen and `pricing_display` (default billing tab) into SubscriptionScreen; `initFeatureFlags()` + `initCrashReporting()` in App.tsx; npm scripts `apk:release` + `distribute:firebase`; doc `apps/mobile/FIREBASE_MVP_TESTING.md`. Verified: typecheck ✅ lint ✅ tests "no tests specified" (stated). ⚠️ YOUR ACTIONS (user): Firebase console — project `bukoo-15ce3` (exists, no new project), add `mvp-testers` group, create RC parameters, enable Crashlytics; `npx firebase login`; native rebuild (expo run:android / EAS) for SDKs to take effect.
- **Task 5 (api.bukoo.id + env)**: complete — route `api.bukoo.id` added to `apps/api/wrangler.jsonc`; URL updated in `eas.json` (3 profiles), `.env`, `.env.example`, `api.ts`, `BookDetailScreen.tsx`. ⚠️ Custom domain must be provisioned in Cloudflare + worker redeployed. ✅(code)
- **Task 6 (Google client ID reconcile)**: complete (code) — resolved conflict: canonical is `576187863248-9voo043m0bm915b8g6b0k1m5ios9qai2` (server client id of Firebase project `bukoo-15ce3`, matching `google-services.json` SHA-1 `5e8f16062ea3cd2c4a0d547876baa6f38cabf625`). Updated eas.json (3), .env, LoginScreen fallback. ⚠️ Manual: ensure Google Cloud Console Android OAuth client has release keystore SHA-1 registered.
- **Task 7 (Apple Sign-In plugin)**: complete — `expo-apple-authentication` added to app.json plugins. ⚠️ Native rebuild required for entitlements.
- **Task 8 (privacy/terms)**: complete — pages already existed at `/privasi` + `/syarat-ketentuan` (verified). ✅
- **Task 9 (informational subscription UI)**: complete — SubscriptionScreen: removed dead UPGRADE CTA, added status banner (from /me) + neutral "Kelola langganan di bukoo.id" footer; ProfileScreen: UPGRADE↗ → "Langganan" + dynamic tier pill, dead SubscriptionModal removed; AiCompanion: dynamic tier badge; BookDetail: is_accessible gating → "Khusus Premium" informational button (no purchase). ✅
- **Task 10 (verify)**: ✅ typecheck mobile/web/api/db all pass; lint mobile clean, api 0 errors (2 pre-existing warnings in untouched files), web changed files clean (repo-wide web lint OOM = infra, known), db no lint script; tests: mobile "no tests specified", web no test script, api vitest 0 test files. Store-compliance scan: no purchase/checkout/payment code in mobile src.

## Commits
- (commit range to be recorded when git commit is made — work is uncommitted on disk)