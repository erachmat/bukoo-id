/**
 * @bukoo/db — public API
 *
 * Re-exports the full schema so consumers can do:
 *   import { users, books, ... } from '@bukoo/db'
 *   import * as schema from '@bukoo/db/schema'
 */
export * from './schema.js';
